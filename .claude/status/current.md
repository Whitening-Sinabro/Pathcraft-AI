**지금**: 하코 문서 2종 재작성용 소스 보강 **완료**. 자막 49편 + 0.5.5 패치노트 판정까지 끝났다. 본문 집필 전이다.

**다음**: ①어느 빌드부터 쓸지 사용자 결정 대기 — 0.5.5 커버리지가 **바라시타 5명 / 화염파(Flameblast) 젬링 2명 / 방패벽 키타바 0명**으로 갈렸다(명부 "발견 2"). 방패벽은 패치가 죽인 게 아니라 크리에이터가 옮겨간 것으로 확인됨. ②집필 시작하면 자막 인용마다 명부 "발견 3" 표로 0.5.5 버그 수정 대조 필수(무기 세트 스냅샷 계열은 무효). ③9/5 패치 후 GGPK 재추출(아래 블로커).

**블로커 없음.** POE2 GGPK 추출이 다시 된다 (2026-09-04 해결).

- 원인은 리버싱 대상이 아니라 **중단된 패치**였다. 8/30 에 인덱스 포함 72,878개 파일을
  큐에 넣고 받다가 연결이 끊겨 있었다. 게임 클라이언트로 다시 받으니 그대로 풀렸다.
- `_.index.bin` 이 정상 번들로 파싱된다 — 113,945,065B 압축 → 147,899,533B, 565 청크,
  compressor=12. **번들 61,214 · 파일 4,227,128 · 디렉토리 95,255 · MurmurHash64A.**
- 추출 결과 **23/24 성공, 총 62,019행**. 실패 1건은 `ScarabTypes`(POE1 전용, POE2 에 없음)로
  4월과 동일하다. 스키마 드리프트 경고는 0.4 스키마로 0.5 데이터를 읽어 나는 것.
- **자동 탐지는 여전히 실패한다** — 폴더명이 `Path of Exile 2 - poe2_production` 이라
  경로를 직접 줘야 한다:
  `src-tauri/target/release/extract_data.exe --game poe2 "<설치경로>" --json`

**단 지금 데이터는 0.5 시점이지 0.5.5 가 아니다.** 0.5.5 신규 영혼핵 17종
(Jiquani's 13 · Atziri's 4)이 `BaseItemTypes` 에 없다 — 영혼핵 자체는 40개 다 있는데
신규만 빠졌다. **9/5 05:00 KST 리그 시작 후 클라이언트가 다시 패치하면 재추출할 것.**
그때 그 17종이 나오면 0.5.5 데이터가 맞고, 하코 문서의 "신규 영혼핵 17종 효과 미확인"
행을 채울 수 있다.

**중재자 한국어명은 이 경로로 못 푼다** — 추출본에 한국어 문자열이 없다(영문 클라이언트).
poe2db/kr 이나 인게임 확인이 필요하다.

**포인터**:
- 하코 소스 명부(조사 방법·자막 49편 대응표·"방패벽은 하나의 빌드가 아니다"·0.5.5 버그수정 대조표·핸들 정정표) → `.claude/status/poe2_hardcore_sources.md`
- 자막 읽기 → `PYTHONIOENCODING=utf-8 python scripts/read_subs.py <file.json3> [검색어…]`
- POE2 가이드·필터·카드 파이프라인(산출물 위치·발행 닥 ID·디스코드 메시지 ID·주입 지뢰) → `.claude/status/poe2_guides.md`
- 0.5.5 패치노트 원문 → `data/_cache/patchnotes/poe2_0_5_5{,_faq,_filter_info,_press_release}.txt`
- 가이드 기준 템플릿 스펙 → `~/.claude/projects/D--Pathcraft-AI/POE2_BUILD_GUIDE_TEMPLATE.md`
