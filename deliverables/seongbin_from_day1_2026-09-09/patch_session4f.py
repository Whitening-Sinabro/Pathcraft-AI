"""Correction after independent adversarial review of the orb-stack finding.

The per-charge step survives, the stream-wide formula does not: the baseline
moves with gear, level and area effects, so the rule is only valid inside one
contiguous gear-and-level segment.
"""
from pathlib import Path

HERE = Path(__file__).resolve().parent

NEW_ROWS = """ (F,6900,'**세케마의 시련** 시련 지도 방 선택 화면이다. 좌측에 LIMIT_LINE·두번째하코·레인하코·송소령·헤예이호기까지 다섯 명의 파티원 생명력 막대가 있고 상단에 명예4148/4148 막대와 방 번호2·5·6이 보인다. 생명력1923/1923, 보호막**3607/3607**, 마나847/847, 정신력7/100이다. 버프 줄은 0%·빨강12·초록7·파랑12에 **금색·보라·자홍 아이콘 세 개가 더 붙어 있다**.','세케마의 시련 파티 진행 확인','**같은 방송의4800·5430초도 빨강12·초록7인데 최대 보호막은3131이다.** 여기서만3607인 것은 시련의 축복·유물 같은 지역 효과가 기준값을 올렸기 때문으로 보이며, 정확한 출처는 이 화면만으로 확정하지 않는다. 하드코어에서 다섯 명 파티로 시련을 도는 장면이다.'),
"""

CORRECTION = """ (C,8620): (None,'같은 화면의 HUD는 생명력1554/1554, 보호막722/722다. 버프 줄 구슬은 빨강15·초록8·파랑6으로9450초와 같은 조합인데 최대치는1512/832로 다르다. **투구 거래 전이라 기준값이 달랐다.**','적대검증이 잡은 반례를 확인해 같은 스택·다른 최대치의 근거로 편입했다. 구슬 스택 규칙을 방송 전체에 하나의 식으로 적용하면 안 된다는 직접 증거다.',[(C,9450),(C,9090),(C,9190)]),\n"""

EVENT_OLD_ID = "'id':'MAXPOOL-ORB-STACKS'"

EVENT_NEW = """ {'id':'MAXPOOL-ORB-STACKS','title':'표시 최대 생명력·보호막이 흔들리는 원인 = 버프 줄 구슬 충전(구간 한정)',
  'frames':[(C,8620),(C,9090),(C,9190),(C,9450),(C,9451),(C,9451.5),(C,9452),(C,9455),(C,9465),(C,9480),(C,9352),(C,9360),(F,120),(F,170),(F,222),(F,4800),(F,5380),(F,5402),(F,5406),(F,5410),(F,5414),(F,5430),(F,5690),(F,6000),(F,6900)],
  'result':'HUD의 표시 최대 생명력·보호막은 **버프 줄의 빨강·초록·파랑 구슬 충전 개수**와 함께 움직인다. 한 방향으로 변하는 단일 변수 근거가 있다. C9451초→9451.5초는 초록8·파랑6이 그대로인 채 빨강만14→15로 오르고 최대 생명력이1490→1512로 딱 그만큼 오른다(빨강 하나당 약22.4). C9190초는 구슬이 하나도 없는 프레임이고 그때 값이1176/670으로, 이것이 그 구간의 기준값이다. 9452초 58레벨 달성 순간에는 구슬이 전부 사라져 기준값1189/670이 그대로 드러난다(레벨업으로 기준 생명력만+13). 초록 충전 하나당 최대 보호막 증가폭은 C에서 약20.25, F에서 약27.9다. **다만 기준값은 상수가 아니다.** 같은 C 방송에서 빨강15·초록8·파랑6이라는 똑같은 조합이 8620초에는1554/722,9090초에는 보호막806,9450초에는1512/832로 나온다. 세 값 모두 초록8개분(162)을 빼면 기준 보호막이560·644·670으로 갈리며, 그 사이에 투구 거래와 장비 교체가 있었다. F도 마찬가지로4800·5430초의 빨강12·초록7은 보호막3131인데,6900초 세케마의 시련에서는 같은 조합에 축복 아이콘 세 개가 더 붙어3607이다. **그러므로 규칙은 장비·레벨·지역 효과가 같은 한 구간 안에서만 성립한다.** 증가폭이 C와 F에서 다른 이유도 여기 있다. F5690초 트리의 주얼 미리보기(에너지 보호막 최대치11% 증가가2936을2997로 만든다)로 역산하면 증가율이 붙지 않은 기준 보호막은 약555이고, 그 5%가 약27.8로 F의 관측 증가폭과 맞는다. 즉 충전은 **증가율이 붙지 않은 기준값에 비례하는 양**을 더하므로 구간마다 표시 증가폭이 달라진다.',
  'remaining':'구슬 세 개의 실제 이름과 효과 문구는 커서를 올린 화면이 없어 미확인이다. 아이콘 형태만 보면 전통적인 지구력(빨강)·격노(초록)·마력(파랑) 충전 구슬과 같지만 이름으로 확정하지 않는다. 이전 판독이 붙인 "플라스크 충전 표시"라는 라벨도 확인된 것이 아니다. F6900초의 추가 보호막476이 시련 축복인지 다른 효과인지도 화면만으로는 확정하지 못했다. 최대 생명력 쪽은 F에서 빨강만으로 설명되지 않는 구간이 남는다.'},
"""

ROW_MAXPOOL = (
    '| **표시되는 최대 생명력·보호막이 구간마다 크게 다르다 — 원인 규명(구간 한정)** | 버프 줄의 빨강·초록·파랑 구슬 충전 개수와 함께 움직인다. '
    'C02:37:31에서 초록·파랑이 그대로인 채 빨강만 14→15로 오르자 최대 생명력이 1490→1512로 딱 그만큼 올랐고, '
    '구슬이 하나도 없는 C02:33:10은 1176/670이다. 58레벨 달성 순간 구슬이 전부 사라져 기준값 1189/670이 드러난다. '
    '증가폭은 C에서 빨강 하나당 약 22.4 생명력·초록 하나당 약 20.25 보호막, F에서 초록 하나당 약 27.9다. '
    '**기준값은 상수가 아니다.** 같은 C 방송에서 빨강15·초록8·파랑6이라는 같은 조합이 722·806·832 세 값으로 갈리고, '
    'F에서도 같은 조합이 3131과 3607(세케마의 시련, 축복 아이콘 3개 추가)로 갈린다 '
    '| **어떤 프레임의 생명력도 그 구간의 기준값으로 쓰지 않는다.** 기준값은 구슬이 0인 프레임이나 트리 화면에서 읽고, '
    '전투 중 표시를 안전 마진으로 오해하지 않는다. 방송 전체에 하나의 식을 적용하면 틀린다. '
    '장비 교체·레벨업·시련 같은 지역 효과가 기준값을 바꾸기 때문이다. 구슬의 이름과 효과 문구는 아직 미확인이다 |'
)

PENDING_MAXPOOL = (
    '| 해결(구간 한정) | 표시되는 최대 생명력·보호막이 왜 구간마다 크게 다른가 | C08620·C09090·C09190·C09450~09480 · F00120~F06900 '
    '| **닫음.** 원인은 위치·정신력 예약·무기 세트가 아니라 **버프 줄의 구슬 충전 개수**다. '
    '단일 변수 근거는 C9451→9451.5초로, 초록·파랑이 고정된 채 빨강만 하나 늘자 최대 생명력이 정확히 그만큼 올랐다. '
    '구슬이 0인 프레임(C09190)의 1176/670이 그 구간의 기준값이고, 58레벨 달성 순간에도 같은 방식으로 기준값이 드러난다. '
    '**다만 기준값은 장비 교체·레벨업·세케마의 시련 같은 지역 효과로 움직이므로 규칙은 한 구간 안에서만 쓴다.** '
    '독립 적대검증이 같은 스택·다른 최대치 반례(C08620 722 / C09090 806 / C09450 832, F04800 3131 / F06900 3607)를 찾아내 '
    '방송 전체에 하나의 식을 적용하던 첫 서술을 정정했다. '
    '**남은 것**은 구슬 세 개의 이름·효과 문구와 F 최대 생명력에 남는 추가 변인, F6900초 추가 보호막476의 출처다 |'
)


def replace_row(text, key, new_row):
    lines = text.split('\n')
    for i, line in enumerate(lines):
        if line.startswith(key):
            lines[i] = new_row
            return '\n'.join(lines), True
    return text, False


def main():
    pf = HERE / 'precision_findings.py'
    src = pf.read_text(encoding='utf-8')
    if 'F,6900' not in src:
        cut = src.rstrip().rfind(']')
        pf.write_text(src[:cut] + NEW_ROWS + src[cut:], encoding='utf-8')
        print('precision row added')
    else:
        print('precision already patched')

    ru = HERE / 'review_updates.py'
    src = ru.read_text(encoding='utf-8')
    if '(C,8620):' not in src:
        close = src.index('}\n\nEVENTS = [')
        src = src[:close] + CORRECTION + src[close:]
    # replace the event block
    start = src.index(EVENT_OLD_ID)
    block_start = src.rfind('{', 0, start)
    block_start = src.rfind('\n', 0, block_start) + 1
    block_end = src.index("'},\n", start) + len("'},\n")
    src = src[:block_start] + EVENT_NEW + src[block_end:]
    ru.write_text(src, encoding='utf-8')
    print('event rewritten')

    rf = HERE / 'research_focus.py'
    src = rf.read_text(encoding='utf-8')
    src, ok1 = replace_row(src, '| **표시되는 최대 생명력·보호막이 구간마다 크게 다르다 — 원인 규명**', ROW_MAXPOOL)
    src, ok2 = replace_row(src, '| 해결 | 표시되는 최대 생명력·보호막이 왜 구간마다 크게 다른가', PENDING_MAXPOOL)
    rf.write_text(src, encoding='utf-8')
    print('research_focus replaced:', ok1, ok2)


if __name__ == '__main__':
    main()
