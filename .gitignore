# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# API Keys & Secrets
.env
*.key
config/api_keys.json

# OAuth Tokens (보안!)
poe_token.json
*.token
src/PathcraftAI.Parser/poe_token.json

# Build data (optional - can be regenerated)
build_data/
game_data/
game_data_poe2/

# Layer 2 independent extractor cache + output (재생성 가능)
_analysis/crosscheck/.cache/
_analysis/crosscheck/tables/
_analysis/crosscheck/report.json

# F0-fix-3 mod name validation report (재생성 가능)
_analysis/mod_name_validation_report.json

# Coach debug dump (S4, D6 해제 조건 관찰용 — 검증 종료 시 build_coach.py 블록과 함께 제거)
_debug/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temp files
temp_*.md
*.tmp

# Logs
*.log
nul

# .NET Build outputs
bin/
obj/
*.dll
*.pdb
*.exe
*.deps.json
*.runtimeconfig.json
*.cache
*.baml
*.g.cs
*.g.resources

# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.vsidx
*.v2
*.backup.json

# Test files
temp_*.xml
temp_*.txt
*.filter
!src/PathcraftAI.Parser/data/**/*.filter

# C# specific
*.AssemblyInfo.cs
*.AssemblyInfoInputs.cache
*.GeneratedMSBuildEditorConfig.editorconfig
*.assets.cache
*.csproj.*.cache
*.csproj.nuget.*
project.assets.json
project.nuget.cache
*.sourcelink.json
*_wpftmp.*

# SQLite databases
*.db
*.db-shm
*.db-wal

# Electron node_modules
node_modules/

# Crash logs
crash_log.txt

# Config (로컬 설정만)
.claude/settings.local.json

# Claude Code 런타임 상태 (로컬 전용)
.claude/scheduled_tasks.lock
.claude/skillmaker/
.claude/status/filter_analysis.md
python/.claude/
src-tauri/.claude/

# Build output
dist/

# External tools/repos (git submodules)
src/PathcraftAI.Parser/pob_repo/
tools/LibGGPK3/
tools/PoE-Overlay-Community-Fork/
tools/awakened-poe-trade/
.env
_archive/
!_archive/phase_f_legacy/
data/atlastree-export/
data/skilltree-export/
data/skilltree-export-poe2/
data/pob_skills_cache/

# Build corpus derived indices (재생성 가능 — GGPK 파이프라인, 69MB)
data/ggpk_derived/

# Claude 런타임 상태가 data/ 로 누수된 것 (로컬 전용)
data/.claude/

# Temp working files (probe stdout, research images 등)
tmp/

# 검증 스크린샷 (재생성 가능)
_analysis/poe2_tree_after_fix.png

# Live client artefacts copied into the workspace (Content.ggpk is ~69GB)
Content.ggpk
HashCache.dat

# 런타임 로그 (재생성 가능)
logs/

# NeverSink 필터 실물 대조본 (제3자 저작물 — 로컬 참조용)
filtersample.txt

# 크래시 덤프
*.stackdump

# 다른 제품 (2026-08-19 D:/kingshot-discord-bot 으로 이동)
kingshot-discord-bot/

# root scratch files from PoB/trade probes
/.tmp_*
